# Tensor class
- Initialize it with an array, shape and stride.
- Move to hip and back to CPU
- perform operations on the class
- for now, just hold a pointer to the thing and include a free_resources function